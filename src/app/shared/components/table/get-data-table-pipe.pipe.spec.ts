import { GetDataTablePipePipe } from './get-data-table-pipe.pipe';

describe('GetDataTablePipePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDataTablePipePipe();
    expect(pipe).toBeTruthy();
  });
});
